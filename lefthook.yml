# Lefthook configuration for Terraform projects
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    terraform-fmt:
      glob: "*.tf"
      run: terraform fmt -recursive
      stage_fixed: true
